生成器函数  生成迭代器

co中不能yield 字符串 bool值

const arrow = ({id,movie})=>{
    console.log(id,movie)
}

中间件
mid1 mid2 mid3

next() 不能忘记写

洋葱模型


纯函数 compose

koa一切的请求都是中间件
通过 koa-compose 结合在一起
每一个中间件都会拿到 http请求的上下文

koa1 => koa2

generator function => async function

express 不是洋葱模型


koa 纯洁  不自带其他中间件

new koa 做了什么事
app.use 做了什么事情
app.listen 做了什么事情